# PRD: keep4mac

> **문서 버전**: v0.1.16 기준 현행화 (2026-05-10)

## 개요

Google Keep과 연동되는 macOS 메뉴바 앱. 언제든 트레이에서 열어 Keep 노트를 조회·작성·편집·삭제할 수 있는 경량 데스크탑 클라이언트.

---

## 배경 및 목적

Google Keep은 웹·모바일 중심 서비스라 macOS에서 빠르게 접근하기 불편하다. 브라우저를 열고 로그인하지 않아도, 메뉴바 아이콘 클릭 한 번으로 노트를 확인하고 내용을 복사해 바로 쓸 수 있는 네이티브 경험을 제공한다.

---

## 목표 사용자

- macOS를 주 개발/업무 환경으로 사용하며 Google Keep에 메모를 남기는 사용자
- 노트 내용을 자주 복사해 다른 곳에 붙여넣는 워크플로를 가진 사용자
- 한국어·영어·일본어 사용자

---

## 구현 완료 기능

### 1. Google 계정 인증
- **Playwright Chromium** 브라우저를 자동 실행해 Google 계정 로그인
- 인증 쿠키(SAPISID)를 macOS Keychain과 `~/.config/keep4mac/session.json`에 저장
- 재실행 시 저장된 세션 자동 복원 (재로그인 불필요)
- Google 앱 비밀번호·별도 API 키 불필요

### 2. 메뉴바(트레이) 상주
- macOS 상단 메뉴바에 SF Symbol 아이콘으로 상주
- 아이콘 클릭 시 노트 목록 패널 즉시 열림
- 마우스 호버 시 아이콘 색상 랜덤 변경 (Google Keep 팔레트)
- Dock에 표시하지 않음 (LSUIElement)

### 3. 사이드바
- 새 노트 / 동기화 / 웹 Keep / 자동시작 토글 / 언어 변경 / 정보 / 로그아웃 / 종료 버튼

### 4. 노트 목록
- Keep 노트 목록을 카드 형태로 표시
- 제목 + 내용 미리보기, 이미지 썸네일 지원
- 핀 고정 노트 상단 배치, 색상 레이블 반영
- 패널 내 검색창으로 제목/내용 실시간 필터링
- 카드 호버 시 📋 버튼으로 제목+내용 클립보드 복사

### 5. 노트 편집기
- 텍스트 노트 인라인 편집 (제목 + 본문)
- 체크리스트 노트 항목별 체크/해제
- 노트 색상 팔레트 변경 (12색)
- 핀 고정/해제 토글
- 이미지 표시 및 삭제
- 노트 내 URL 자동 인식 및 클릭으로 브라우저 오픈
- 노트 삭제

### 6. 노트 생성
- 사이드바 "새 노트" 버튼으로 빈 텍스트 노트 생성
- 제목 + 본문 입력 후 저장, 색상/핀 설정 지원

### 7. 자동 시작
- macOS LaunchAgent 등록/해제로 로그인 시 자동 실행 토글

### 8. 다국어 지원
- 한국어·영어·일본어 지원 (gettext `.po`/`.mo` 파일)
- 시스템 언어 자동 감지, 사이드바에서 런타임 즉시 전환
- 선택 언어는 `~/.config/keep4mac/settings.json`에 저장

### 9. About 다이얼로그
- 버전 정보, 제작자 GitHub 링크, 저장소 링크

### 10. 배포
- PyInstaller로 단일 `.app` 번들 + ad-hoc 코드서명
- DMG 패키징 (drag-to-install)
- GitHub Actions 태그 푸시 시 자동 빌드 및 릴리즈 첨부

---

## 구현하지 않기로 결정한 기능

| 기능 | 이유 |
|------|------|
| 백그라운드 자동 동기화 | 비공식 API 부하 우려, 수동 동기화로 충분 |
| 라벨(태그) 관리 | 범위 초과 |
| 협업·공유 노트 | 범위 초과 |
| 알림·리마인더 | 범위 초과 |

---

## 비기능 요구사항

| 항목 | 기준 |
|------|------|
| 패널 열림 응답 | 500ms 이내 |
| 지원 OS | macOS 12 Monterey 이상 |
| 인증 정보 저장 | macOS Keychain (평문 저장 금지) |
| 라이선스 | GNU GPL v3 (PyQt6 GPL 조건 충족) |

---

## 기술 스택

| 영역 | 선택 | 이유 |
|------|------|------|
| 언어 | Python 3.13 | gkeepapi 생태계 |
| Keep 연동 | gkeepapi (비공식) + SAPISIDHASH | 유일한 Keep API 라이브러리 |
| 브라우저 로그인 | Playwright (Chromium) | Google 로그인 자동화 |
| GUI | PyQt6 6.7.1 | 풍부한 위젯, macOS 호환성 |
| 메뉴바 | rumps + PyObjC | macOS 네이티브 트레이 직접 제어 |
| 세션 저장 | keyring (macOS Keychain) | 보안 자격증명 관리 |
| 다국어 | gettext (표준 라이브러리) | .po/.mo 파일 기반, 런타임 전환 |
| 패키징 | PyInstaller 6.x | macOS .app 번들 생성 |

---

## 화면 구성

```
[메뉴바 아이콘] 클릭
        ↓
┌────┬───────────────────────┐
│🗒  │ 🔍 검색…              │  ← 노트 목록 화면
│↻   ├───────────────────────┤
│🌐  │ 📌 고정됨             │
│🚀  │ ┌─────────────────┐   │
│🌏  │ │ 핀 노트 제목    │   │
│?   │ │ 내용 미리보기…  │📋 │
│↩   │ └─────────────────┘   │
│✕   ├───────────────────────┤
│    │ 메모                  │
│    │ ┌─────────────────┐   │
│    │ │ 노트 제목       │   │
│    │ │ 내용 미리보기…  │📋 │
│    │ └─────────────────┘   │
└────┴───────────────────────┘

노트 선택 시 → 편집 화면
┌────┬───────────────────────┐
│    │ ←  노트 편집      🗑  │
│    ├───────────────────────┤
│    │ 제목                  │
│    │ ───────────────────   │
│    │                       │
│    │ 본문 내용             │
│    │ (편집 가능)           │
│    ├───────────────────────┤
│    │ 🎨🎨🎨  📌   [저장]  │
└────┴───────────────────────┘
```

---

## 향후 계획 (오픈 이슈)

| 이슈 | 내용 | 구분 |
|------|------|------|
| [#25](../../issues/25) | 노트를 Markdown 파일로 저장 | enhancement |
| [#26](../../issues/26) | 노트를 카카오톡으로 공유 | enhancement |
| [#27](../../issues/27) | 노트를 이메일로 보내기 | enhancement |
| [#28](../../issues/28) | 삭제 아이콘 스타일 통일 | enhancement |
| [#29](../../issues/29) | 노트 삭제 시 확인 다이얼로그 | enhancement |
| [#30](../../issues/30) | 설정 아이콘 추가 및 기능 재배치 | enhancement |
| [#32](../../issues/32) | 노트 번역해서 보기 | enhancement |
| [#33](../../issues/33) | 이미지 가로 크기 초과 버그 수정 | bug |
| [#34](../../issues/34) | 사이드바 버튼 텍스트 줄바꿈 버그 수정 | bug |
