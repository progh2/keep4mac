# PRD: keeptray

> **문서 버전**: v0.1.81 기준 현행화 (2026-05-13)

## 개요

Google Keep과 연동되는 macOS · Windows 데스크탑 트레이 앱. 언제든 트레이에서 열어 Keep 노트를 조회·작성·편집·삭제할 수 있는 경량 크로스플랫폼 클라이언트.

---

## 배경 및 목적

Google Keep은 웹·모바일 중심 서비스라 macOS에서 빠르게 접근하기 불편하다. 브라우저를 열고 로그인하지 않아도, 메뉴바 아이콘 클릭 한 번으로 노트를 확인하고 내용을 복사해 바로 쓸 수 있는 네이티브 경험을 제공한다.

---

## 목표 사용자

- macOS 또는 Windows를 주 개발/업무 환경으로 사용하며 Google Keep에 메모를 남기는 사용자
- 노트 내용을 자주 복사해 다른 곳에 붙여넣는 워크플로를 가진 사용자
- 한국어·영어·일본어 사용자

---

## 구현 완료 기능

### 1. Google 계정 인증
- **Playwright Chromium** 브라우저를 자동 실행해 Google 계정 로그인
- 인증 쿠키(SAPISID)를 macOS Keychain / Windows 자격 증명 관리자와 `~/.config/keeptray/session.json`에 저장
- 재실행 시 저장된 세션 자동 복원 (재로그인 불필요)
- Google 앱 비밀번호·별도 API 키 불필요

### 2. 메뉴바(트레이) 상주
- macOS 상단 메뉴바에 SF Symbol 아이콘으로 상주
- 아이콘 좌클릭 시 패널 열기/닫기 토글
- 아이콘 우클릭 시 종료 메뉴 표시 (macOS)
- 마우스 호버 시 아이콘 색상 랜덤 변경 (Google Keep 팔레트)
- Dock에 표시하지 않음 (LSUIElement)

### 3. 플로팅 독립 윈도우
- 다른 앱 포커스 시에도 패널 유지 (NSPanel `setHidesOnDeactivate_(False)`)
- 상단 드래그 바로 화면 어디서든 이동 가능
- ✕ 버튼으로 패널 닫기
- 패널 위치 자동 저장 및 다음 실행 시 복원

### 4. 사이드바
- **상단**: 새 노트(🗒) / 동기화(🔄) 버튼
- **중단 스크롤 영역**: Google Keep 라벨 목록 — 라벨 버튼 클릭으로 노트 필터링, 재클릭 시 해제
- **하단**: 보관함(📦) / 휴지통(🗑) / 설정(⚙️) 버튼
- **설정 메뉴**: 웹 Keep 열기 / 자동시작 토글 / 언어 변경 / 폰트 설정 / 라벨 관리 / 테마 / 내 메일 주소 / 정보 / 로그아웃 / 종료

### 5. 노트 목록
- Keep 노트 목록을 카드 형태로 표시
- 제목 + 내용 미리보기, 이미지 썸네일 지원
- 핀 고정 노트 상단 배치 및 📌 아이콘 표시, 색상 레이블 반영
- 패널 내 검색창으로 제목/내용 실시간 필터링
- **색상 필터**: 검색창 하단 팔레트(11색)에서 색상 클릭 시 해당 색 노트만 표시, 재클릭으로 해제; 라벨 필터·텍스트 검색과 동시 적용
- **정렬**: 수정일(최신/오래된순) · 생성일(최신/오래된순) · 제목(A→Z, Z→A) 6가지 옵션
- 카드 호버 시 📋 버튼으로 제목+내용 클립보드 복사
- 편집기 복귀 시 목록 즉시 갱신 (메모리 캐시 자동 반영)
- **오프라인 배너**: 동기화 실패 시 주황색 배너("⚠ Offline — Showing cached notes") 표시, 캐시 노트 계속 사용 가능
- **세션 만료 배너**: AuthError 감지 시 빨간 배너("🔒 Session expired") 표시 및 재로그인 안내 다이얼로그 자동 표시

### 6. 노트 편집기
- 텍스트 노트 인라인 편집 (제목 + 본문)
- 체크리스트 노트 항목별 체크/해제
- 노트 색상 팔레트 변경 (12색), 변경 즉시 목록 카드에 반영
- 핀 고정/해제 토글
- **보관(📦)** — 편집기에서 직접 보관함으로 이동 (자동 저장 후 이동)
- **라벨(🏷)** — 현재 노트에 라벨 할당·해제 (팝업 메뉴로 토글)
- 이미지 표시 및 삭제
- 노트 내 URL 자동 인식 및 클릭으로 브라우저 오픈
- 노트 삭제 (확인 다이얼로그 포함)
- **자동 저장** — 뒤로가기·ESC·패널 재열기 시 변경사항 자동 저장
- **되돌리기(↩)** — 수정 감지 시 footer에 버튼 표시, 클릭으로 마지막 저장 상태 복원
- **편집기 클립보드 복사(📋)** — 제목+본문 전체 복사 후 토스트 표시
- **한국어 IME 최적화** — 포커스 시 IME 재초기화로 첫 글자 자소 분리 방지

### 7. 노트 생성
- 사이드바 "새 노트" 버튼으로 빈 텍스트 노트 생성
- **클립보드로 새 노트**: "새 노트" 버튼 우클릭 시 컨텍스트 메뉴 표시, 클립보드에 텍스트가 있으면 "클립보드로 새 노트 만들기" 옵션 제공 — 본문에 클립보드 텍스트 자동 삽입
- 제목 + 본문 입력, 색상/핀 설정 지원
- 뒤로가기 시 내용 있으면 자동 저장

### 8. 보관함
- 사이드바 📦 버튼으로 보관함 화면 진입
- 보관된 노트 목록 카드 형태로 표시 (제목 + 내용 미리보기)
- **↩ 보관 해제** — 노트를 일반 목록으로 복원

### 9. 휴지통
- 사이드바 🗑 버튼으로 휴지통 화면 진입
- 삭제된 노트 목록 카드 형태로 표시
- **↩ 복원** — 노트를 일반 목록으로 복원
- **✕ 영구 삭제** — 확인 다이얼로그 후 완전 삭제
- **휴지통 비우기** — 전체 영구 삭제 (확인 다이얼로그 포함)

### 10. 라벨 관리
- 설정 메뉴 → "라벨 관리…" 다이얼로그
- 라벨 추가 (이름 입력 + Add 버튼 또는 Enter)
- 라벨 이름 변경 (인라인 텍스트 편집, 포커스 이탈 시 자동 저장)
- 라벨 삭제 (확인 다이얼로그 포함)

### 11. 폰트 설정
- 설정 메뉴 → "폰트 설정…" 모달리스 다이얼로그
- 4개 영역별 독립 설정: 목록 제목 / 목록 내용 / 편집기 제목 / 편집기 본문
- 각 영역에서 폰트 종류·크기 변경, 실시간 미리보기
- "기본값으로" 버튼으로 전체 초기화

### 12. 내보내기 · 공유
- **파일 저장**: Markdown (.md) / Text (.txt) / Image (.png) / PDF (.pdf) / 한글 (.hwpx, 이미지 포함) / Word (.docx, 이미지 포함)
- **이메일 공유**: 기본 메일 앱으로 `mailto:` 실행 (제목 형식: `keeptray - {노트 제목}`)
- **내 메일로 보내기**: 설정에 저장된 내 메일 주소로 원클릭 전송
- **카카오톡 공유**: 내용 클립보드 복사 후 카카오톡 실행
- **번역 새 노트**: MyMemory 무료 API로 번역 후 새 노트 생성 (한·영·일 상호 번역)

### 13. 테마
- **라이트·다크·시스템** 세 가지 테마 지원
- 시스템 테마: macOS NSAppearance 또는 QPalette 밝기로 자동 감지
- 설정 메뉴 → 테마 서브메뉴에서 즉시 전환
- 패널 배경, 노트 목록 검색창/정렬 버튼, 스크롤 영역 등에 테마 색상 적용

### 14. 설정 저장
- `~/.config/keeptray/settings.json`에 내 메일 주소·선택 언어·패널 위치·폰트 설정·테마 저장
- 앱 재시작 후에도 유지

### 15. 자동 시작
- macOS: LaunchAgent 등록/해제로 로그인 시 자동 실행 토글
- Windows: 레지스트리 `HKCU\...\Run` 키 등록/해제

### 16. 다국어 지원
- 한국어·영어·일본어 지원 (gettext `.po`/`.mo` 파일)
- 시스템 언어 자동 감지, 사이드바에서 런타임 즉시 전환

### 17. About 다이얼로그
- 버전 정보, 제작자 GitHub 링크, 저장소 링크

### 18. 자동 업데이트
- 앱 실행 시 GitHub Releases API로 최신 버전 감지
- 새 버전 발견 시 패널 상단에 업데이트 알림 배너 표시
- "지금 업데이트" 클릭 시 플랫폼별 파일(.dmg / .zip) 다운로드 후 설치
- 진행률 표시 (다운로드 중… / 설치 중…)

### 19. 배포
- macOS: PyInstaller `.app` 번들 + ad-hoc 코드서명 + DMG 패키징
- Windows: PyInstaller `.exe` + ZIP 패키징 (첫 실행 시 Chromium 자동 다운로드)
- GitHub Actions 태그 푸시 시 macOS/Windows 동시 빌드 및 릴리즈 자동 첨부

---

## 구현하지 않기로 결정한 기능

| 기능 | 이유 |
|------|------|
| 백그라운드 자동 동기화 | 비공식 API 부하 우려, 수동 동기화로 충분 |
| 협업·공유 노트 | 범위 초과 |
| 알림·리마인더 | 범위 초과 |
| 라벨 순서 변경 | Google Keep API에서 라벨 순서를 지원하지 않음 |

---

## 비기능 요구사항

| 항목 | 기준 |
|------|------|
| 패널 열림 응답 | 500ms 이내 |
| 지원 OS | macOS 12 Monterey 이상, Windows 10 이상 |
| 인증 정보 저장 | macOS Keychain / Windows 자격 증명 관리자 (평문 저장 금지) |
| 라이선스 | GNU GPL v3 (PyQt6 GPL 조건 충족) |

---

## 기술 스택

| 영역 | 선택 | 이유 |
|------|------|------|
| 언어 | Python 3.11 이상 | gkeepapi 생태계 |
| Keep 연동 | gkeepapi (비공식) + SAPISIDHASH | 유일한 Keep API 라이브러리 |
| 브라우저 로그인 | Playwright (Chromium) | Google 로그인 자동화 |
| GUI | PyQt6 6.7.1 | 풍부한 위젯, 크로스플랫폼 |
| 트레이 (macOS) | rumps + PyObjC | macOS 네이티브 메뉴바 직접 제어 |
| 트레이 (Windows) | pystray + Pillow | Windows 시스템 트레이 |
| 세션 저장 | keyring | macOS Keychain / Windows 자격 증명 관리자 |
| 문서 저장 | python-docx, python-hwpx | Word·한글 형식 내보내기 |
| 번역 | MyMemory 무료 API | 별도 API 키 불필요 |
| 다국어 | gettext (표준 라이브러리) | .po/.mo 파일 기반, 런타임 전환 |
| 패키징 | PyInstaller 6.x | macOS .app / Windows .exe 번들 생성 |

---

## 화면 구성

```
[메뉴바 아이콘] 클릭
        ↓
┌────┬───────────────────────┐
│🗒  │ 🔍 검색…          ⇅  │  ← 노트 목록 화면
│🔄  ├───────────────────────┤
│────│ 🔴🟠🟡🟢🔵🟣…(색상필터)│
│    ├───────────────────────┤
│    │ 📌 고정됨             │
│🏷  │ ┌─────────────────┐   │
│(라 │ │📌 핀 노트 제목  │   │
│벨  │ │ 내용 미리보기…  │📋 │
│목  │ └─────────────────┘   │
│록) ├───────────────────────┤
│────│ 메모                  │
│📦  │ ┌─────────────────┐   │
│🗑  │ │ 노트 제목       │   │
│⚙️  │ │ 내용 미리보기…  │📋 │
│    │ └─────────────────┘   │
└────┴───────────────────────┘

노트 선택 시 → 편집 화면
┌────┬──────────────────────────────┐
│    │ ←  노트 편집                 │
│    ├──────────────────────────────┤
│    │ 제목                         │
│    │ ──────────────────────       │
│    │                              │
│    │ 본문 내용                    │
│    │ (편집 가능)                  │
│    ├──────────────────────────────┤
│    │ 🎨🎨🎨  📌  📦  ✕  📋  🏷  📤 ↩│
└────┴──────────────────────────────┘
```

---

## 향후 계획

현재 확정된 미구현 이슈 없음. 사용자 피드백에 따라 신규 이슈 등록 후 진행.
